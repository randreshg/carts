#!/usr/bin/env sh
ROOT=$(CDPATH= cd -- "$(dirname "$0")/../.." && pwd)
TOOL=$(basename "$0")
PYTHON="$ROOT/.dekk/env/bin/python3"
if [ ! -x "$PYTHON" ]; then
  PYTHON=python3
fi
exec "$PYTHON" "$ROOT/tools/scripts/managed_tool.py" "$TOOL" "$@"
