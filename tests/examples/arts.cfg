[ARTS]
# Default ARTS configuration for examples
threads=16
tMT=0

# Network configuration
outgoing=1
incoming=1
ports=1

# Launcher configuration
launcher=ssh
masterNode=localhost
nodeCount=1
nodes=localhost

# Performance monitoring
counterFolder=./counters/
counterStartPoint=1

