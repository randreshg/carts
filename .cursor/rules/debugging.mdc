---
description: How to run or debug carts
alwaysApply: false
---
# Debugging CARTS

## Overview
Debugging CARTS involves understanding four main components:
- **Polygeist**: Front-end infrastructure (external/Polygeist)
- **LLVM/MLIR**: Compiler infrastructure (external/Polygeist/llvm-project)  
- **ARTS**: Runtime system (external/arts)
- **CARTS**: Core infrastructure (include/, lib/, tools/)

**Note**: Always refer to `.cursor/rules/build-and-compilation.mdc` for build system details.

**Cleaning and rebuild guidance**
- Run `carts clean` only within the specific example/test directory you are debugging (e.g., `tests/test/tasksimple`).
- Do not rebuild the entire CARTS project unless you have modified CARTS code or build files.

## Step-by-Step Compilation Pipeline

### 1. C++ to MLIR Conversion
Convert C++ source to MLIR using Polygeist:
```bash
carts cgeist simple.cpp -std=c++17 -fopenmp -O0 -S > simple.mlir
```

### 2. MLIR Optimization and ARTS Conversion
Execute the compilation pipeline step by step. For debugging, examine `tools/run/carts-run.cpp` to understand the complete pipeline.

#### Step-by-Step Debugging Approach
Create intermediate files after each major transformation pass to isolate issues:

**First Major Pass - OpenMP to ARTS:**
```bash
carts opt simple.mlir \
  --lower-affine \
  --cse \
  --polygeist-mem2reg \
  --canonicalize \
  --loop-invariant-code-motion \
  --arts-inliner \
  --convert-openmp-to-arts > simple-arts.mlir
```

**Second Major Pass - EDT Pass:**
```bash
carts opt simple-arts.mlir \
  --symbol-dce \
  --edt > simple-edt.mlir
```

**Continue with remaining passes:**
Examine `tools/run/carts-run.cpp` to understand the complete pipeline and identify which other passses to run

#### Debugging Pass-Specific Issues
Enable debug output for specific passes when encountering errors:
```bash
# Replace dashes with underscores in debug flag names
carts opt simple.mlir \
  --lower-affine \
  --cse \
  --polygeist-mem2reg \
  --canonicalize \
  --loop-invariant-code-motion \
  --arts-inliner \
  --convert-openmp-to-arts \
  -debug-only=convert_openmp_to_arts > simple-arts.mlir
```

**Common Debug Flags:**
- `-debug-only=convert_openmp_to_arts`
- `-debug-only=edt`
- `-debug-only=create_db`
- `-debug-only=convert_arts_to_llvm`

### 3. LLVM IR Generation
Convert final MLIR to LLVM IR:
```bash
carts mlir-translate --mlir-to-llvmir simple-final.mlir > simple-arts.ll
```

**Troubleshooting LLVM Generation:**
- Analyze error messages carefully
- Check ARTS-to-LLVM conversion passes
- Verify MLIR structure before conversion

### 4. Final Compilation
Compile LLVM IR to executable:
```bash
carts compile simple-arts.ll -o simple
```

### 5. Execution and Runtime Debugging
Ensure `arts.cfg` configuration exists, then run:
```bash
./simple
```

**Runtime Issue Resolution:**
- **Segmentation Faults**: Examine the generated `.ll` code for incorrect ARTS function calls
- **Unknown ARTS Functions**: Check `external/arts` source files for correct function signatures
- **Code Generation Issues**: Return to ARTS-to-LLVM conversion passes and re-run with fixes

## Debugging Workflow Summary
1. **Isolate**: Use step-by-step pipeline to identify failing pass
2. **Debug**: Enable pass-specific debug output
3. **Analyze**: Examine intermediate MLIR/LLVM files
4. **Verify**: Check ARTS runtime API usage
5. **Iterate**: Fix issues and re-run pipeline

# Debugging CARTS

## Overview
Debugging CARTS involves understanding four main components:
- **Polygeist**: Front-end infrastructure (external/Polygeist)
- **LLVM/MLIR**: Compiler infrastructure (external/Polygeist/llvm-project)  
- **ARTS**: Runtime system (external/arts)
- **CARTS**: Core infrastructure (include/, lib/, tools/)

**Note**: Always refer to `.cursor/rules/build-and-compilation.mdc` for build system details.

## Step-by-Step Compilation Pipeline

### 1. C++ to MLIR Conversion
Convert C++ source to MLIR using Polygeist:
```bash
carts cgeist simple.cpp -std=c++17 -fopenmp -O0 -S > simple.mlir
```

### 2. MLIR Optimization and ARTS Conversion
Execute the compilation pipeline step by step. For debugging, examine `tools/run/carts-run.cpp` to understand the complete pipeline.

#### Step-by-Step Debugging Approach
Create intermediate files after each major transformation pass to isolate issues:

**First Major Pass - OpenMP to ARTS:**
```bash
carts opt simple.mlir \
  --lower-affine \
  --cse \
  --polygeist-mem2reg \
  --canonicalize \
  --loop-invariant-code-motion \
  --arts-inliner \
  --convert-openmp-to-arts > simple-arts.mlir
```

**Second Major Pass - EDT Pass:**
```bash
carts opt simple-arts.mlir \
  --symbol-dce \
  --edt > simple-edt.mlir
```

**Continue with remaining carts passes:**
Examine `tools/run/carts-run.cpp` to understand the complete pipeline and identify which other passses to run.
You must run all arts related passes and group the non-arts related passes.

#### Debugging Pass-Specific Issues
Enable debug output for specific passes when encountering errors:
```bash
# Replace dashes with underscores in debug flag names
carts opt simple.mlir \
  --lower-affine \
  --cse \
  --polygeist-mem2reg \
  --canonicalize \
  --loop-invariant-code-motion \
  --arts-inliner \
  --convert-openmp-to-arts \
  -debug-only=convert_openmp_to_arts > simple-arts.mlir
```

**Common Debug Flags:**
- `-debug-only=convert_openmp_to_arts`
- `-debug-only=edt`
- `-debug-only=create_db`
- `-debug-only=convert_arts_to_llvm`

### 3. LLVM IR Generation
Convert final MLIR to LLVM IR:
```bash
carts mlir-translate --mlir-to-llvmir simple-final.mlir > simple-arts.ll
```

**Troubleshooting LLVM Generation:**
- Analyze error messages carefully
- Check ARTS-to-LLVM conversion passes
- Verify MLIR structure before conversion

### 4. Final Compilation
Compile LLVM IR to executable:
```bash
carts compile simple-arts.ll -o simple
```

### 5. Execution and Runtime Debugging
Ensure `arts.cfg` configuration exists, then run:
```bash
./simple
```

**Runtime Issue Resolution:**
- **Segmentation Faults**: Examine the generated `.ll` code for incorrect ARTS function calls
- **Unknown ARTS Functions**: Check `external/arts` source files for correct function signatures
- **Code Generation Issues**: Return to ARTS-to-LLVM conversion passes and re-run with fixes

## Debugging Workflow Summary
1. **Isolate**: Use step-by-step pipeline to identify failing pass
2. **Debug**: Enable pass-specific debug output
3. **Analyze**: Examine intermediate MLIR/LLVM files
4. **Verify**: Check ARTS runtime API usage
5. **Iterate**: Fix issues and re-run pipeline

