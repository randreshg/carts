# CODIR Include Target

Target home for the CODIR dialect public headers and TableGen files.

CODIR owns isolated codelet IR, explicit deps, scalar params, token-local
memref views, yielded values, and verifier rules that prevent implicit captures
from above.
